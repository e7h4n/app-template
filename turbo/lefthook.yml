pre-commit:
  parallel: true
  commands:
    lint:
      run: cd turbo && pnpm turbo run lint
      glob: "turbo/**/*.{js,ts,tsx,jsx,mjs,cjs}"
    prettier:
      run: cd turbo && pnpm prettier --list-different --ignore-unknown {staged_files}
      glob: "turbo/**/*"
